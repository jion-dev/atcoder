#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (int)(n); i++)

int calc(char c, int t) {
  int index = t % 10 + 1;
  return index - 1;
}

int main() {
  int N;
  cin >> N;

  vector<string> s(N);
  for(auto &nx : s) {
    cin >> nx;
  }

  int minutes = 0;
  bool can = false;
  while(!can) {

    rep(i, N) {}
  }
}
